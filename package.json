{
  "name": "team-task-manager",
  "version": "1.0.0",
  "private": true,
  "description": "Team Task Manager: full-stack project and task tracker (React, Express, MySQL).",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "install:all": "npm install && npm --prefix server install && npm --prefix client install",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "dev": "npm-run-all --parallel dev:server dev:client",
    "build:client": "npm --prefix client run build",
    "build:server": "npm --prefix server install --omit=dev",
    "build": "npm --prefix server install && npm --prefix client install && npm --prefix client run build",
    "migrate": "npm --prefix server run migrate",
    "seed": "npm --prefix server run seed",
    "start": "npm --prefix server start",
    "postinstall": "echo \"Run 'npm run install:all' to install workspace dependencies.\""
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
